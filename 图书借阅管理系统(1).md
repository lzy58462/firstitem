# 图书借阅管理系统


## 一、数据结构

```c
// 图书
typedef struct book {
    char isbn[20];          // ISBN（主键）
    char title[100];        // 书名
    char author[50];        // 作者
    char publisher[50];     // 出版社
    int total;              // 馆藏总数
    int borrowed;           // 已借出数量
    int borrowCount;        // 累计借阅次数
    struct book* next;
} Book;

// 读者
typedef struct reader {
    char id[20];            // 借阅证号（主键）
    char name[50];          // 姓名
    char phone[15];         // 电话
    int borrowedCount;      // 当前已借数量（上限5本）
    struct reader* next;
} Reader;

// 借阅记录
typedef struct record {
    char readerId[20];
    char isbn[20];
    time_t borrowDate;      // 借书日期
    int returned;           // 0未还 1已还
    struct record* next;
} Record;
```


## 二、功能要求

### 图书管理
1. **添加图书**：ISBN不能重复，total≥1，borrowed和borrowCount初始为0
2. **删除图书**：按ISBN删除，**borrowed>0时禁止删除**
3. **修改图书**：按ISBN修改，修改后total不能小于borrowed
4. **查询图书**：按ISBN精确查询 / 按书名或作者模糊查询
5. **显示全部**：按borrowCount降序排列

### 读者管理
6. **添加读者**：证号不能重复，borrowedCount初始为0
7. **删除读者**：按证号删除，**borrowedCount>0时禁止删除**
8. **修改读者**：按证号修改姓名、电话
9. **查询读者**：按证号或姓名查询，同时显示该读者当前所借图书
10. **显示全部读者**：表格列出所有读者

### 借阅管理
11. **借书**：输入证号和ISBN，校验读者存在、图书存在、读者未借满5本、图书有库存(borrowed < total)，全部通过后borrowedCount+1、borrowed+1、borrowCount+1、创建借阅记录记录当前时间
12. **还书**：输入证号和ISBN，校验读者存在、图书存在、该读者确实借了该书，通过后borrowedCount-1、borrowed-1、借阅记录标记为已还，计算借阅天数，**超过30天显示逾期提示**
13. **借阅记录**：显示所有未归还记录（读者姓名、书名、借书日期、是否逾期）
14. **逾期记录**：显示所有逾期未还记录及逾期天数

### 统计功能
15. **系统统计**：图书总数、总馆藏量、已借出总数、可借总数、读者总数、当前有借书的读者数

### 文件操作
16. **自动加载**：程序启动时从 books.dat、readers.dat、records.dat 读取数据
17. **自动保存**：程序退出时自动保存数据到上述三个文件


## 三、菜单结构

```
1. 添加图书
2. 删除图书
3. 修改图书
4. 查询图书
5. 显示全部图书
6. 添加读者
7. 删除读者
8. 修改读者
9. 查询读者
10. 显示全部读者
11. 借书
12. 还书
13. 查看借阅记录
14. 查看逾期记录
15. 系统统计
16. 退出并保存
```


## 四、技术要求

| 项目     | 要求                                       |
| -------- | ------------------------------------------ |
| 数据结构 | 三个链表（Book/Reader/Record）             |
| 内存管理 | malloc动态分配，退出前全部free             |
| 文件格式 | 二进制文件（.dat），fread/fwrite           |
| 时间处理 | `<time.h>`，记录借书日期，计算逾期         |
| 输入校验 | 主键不能重复、总数≥1、借书上限5、库存充足  |
| 错误处理 | 所有异常给出明确提示，程序不能崩溃         |
| 代码规范 | 函数拆分、缩进统一、关键注释               |
| 主函数   | 只放菜单循环和分支调用，具体实现在其他函数 |


## 五、核心函数清单（必须实现）

```c
// 文件操作
void loadData(Book** bHead, Reader** rHead, Record** recHead);
void saveData(Book* bHead, Reader* rHead, Record* recHead);

// 图书操作
void addBook(Book** head);
void deleteBook(Book** head);
void modifyBook(Book* head);
void searchBook(Book* head);
void displayBooks(Book* head);

// 读者操作
void addReader(Reader** head);
void deleteReader(Reader** head);
void modifyReader(Reader* head);
void searchReader(Reader* head, Record* recHead, Book* bHead);
void displayReaders(Reader* head);

// 借阅操作
void borrowBook(Book* bHead, Reader* rHead, Record** recHead);
void returnBook(Book* bHead, Reader* rHead, Record** recHead);
void viewRecords(Record* recHead, Reader* rHead, Book* bHead);
void viewOverdue(Record* recHead, Reader* rHead, Book* bHead);

// 统计
void systemStats(Book* bHead, Reader* rHead);

// 工具
int findBook(Book* head, char* isbn);
int findReader(Reader* head, char* id);
void freeAll(Book* bHead, Reader* rHead, Record* recHead);
```


## 七、测试用例（自测用）

| 测试场景           | 预期结果                     |
| ------------------ | ---------------------------- |
| 添加ISBN重复的图书 | 提示"ISBN已存在"             |
| 删除有借出的图书   | 提示"该书尚有借出，不能删除" |
| 读者借第6本书      | 提示"已达到借阅上限(5本)"    |
| 借库存为0的书      | 提示"该书已全部借出"         |
| 借书后查看记录     | 显示借阅记录及日期           |
| 还书时借阅天数>30  | 显示"逾期XX天"               |
| 程序退出再启动     | 数据完整恢复                 |
